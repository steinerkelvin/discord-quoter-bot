// Generated by CoffeeScript 1.12.4
(function() {
  var subscript_numbers;

  subscript_numbers = new Map([["0", "₀"], ["1", "₁"], ["2", "₂"], ["3", "₃"], ["4", "₄"], ["5", "₅"], ["6", "₆"], ["7", "₇"], ["8", "₈"], ["9", "₉"]]);

  module.exports = {
    idt: function(i) {
      return i;
    },
    get_env: function(names) {
      var j, len, name, value, vars;
      vars = {};
      for (j = 0, len = names.length; j < len; j++) {
        name = names[j];
        if (!(value = process.env[name])) {
          console.warn("'" + name + "' environment variable missing");
          vars[name] = null;
        } else {
          vars[name] = value;
        }
      }
      return vars;
    },
    get_env_check: function(names) {
      var j, len, name, value, vars;
      vars = {};
      for (j = 0, len = names.length; j < len; j++) {
        name = names[j];
        if (!(value = process.env[name])) {
          console.error("'" + name + "' environment variable missing");
          process.exit(1);
        } else {
          vars[name] = value;
        }
      }
      return vars;
    },
    isTrue: function(v) {
      var ref;
      if (typeof v === 'string' && ((ref = v.toLowerCase()) === 'true' || ref === '1')) {
        return true;
      } else if (v === true || v === 1) {
        return true;
      } else {
        return false;
      }
    },
    strNumberToSubscript: function(string) {
      var c;
      return ((function() {
        var j, len, results;
        results = [];
        for (j = 0, len = string.length; j < len; j++) {
          c = string[j];
          results.push(subscript_numbers.has(c) ? subscript_numbers.get(c) : c);
        }
        return results;
      })()).join("");
    }
  };

}).call(this);

//# sourceMappingURL=util.js.map
